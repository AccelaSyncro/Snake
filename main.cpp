#include "Snake.h"
int main() {
	Snake s;
	s.startGame();
	//const int gameWidth = 80;
	//const int gameHeight = 30;
	//auto gameMap = new unsigned char[gameWidth * gameHeight];

	//int limit = 0;
	//while (true) {
	//	if (limit == 40) { limit = 0; }
	//	for (int x = 0; x < gameWidth; x++)
	//	{
	//		for (int y = 0; y < gameHeight; y++)
	//		{
	//			if (x == limit || x == gameWidth - limit || y == limit || y == gameHeight - limit) {
	//				gameMap[x + y * gameWidth] = 9;
	//			}
	//			else
	//			{
	//				gameMap[x + y * gameWidth] = 0;
	//			}
	//		}
	//	}
	//	wc.display(gameMap, gameWidth, gameHeight);
	//	limit++;
	//	Sleep(100);
	//}
}